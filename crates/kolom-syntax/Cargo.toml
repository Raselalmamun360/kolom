[package]
name = "kolom-syntax"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
kolom-lexer = { path = "../kolom-lexer" }

[dev-dependencies]
# The widget-name tables must stay NFC; parser.rs asserts it.
unicode-normalization = "0.1"
