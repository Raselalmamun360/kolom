ইম্পোর্ট পাথ

অ্যাপ {

    লেখো(পাথ.ফাইলনাম(পাথ.জোড়ো("এক", "দুই.txt")))

    লেখো(পাথ.ফাইলনাম("এক/দুই/তিন.txt"))

    লেখো(পাথ.ডিরেক্টরিনাম("এক/দুই/তিন.txt"))

    লেখো(পাথ.এক্সটেনশন("এক/দুই/তিন.txt"))

    লেখো(পাথ.এক্সটেনশন("এক/.gitignore"))

    লেখো(পাথ.এক্সটেনশন("এক/তিন"))

    লেখো(লেখায়(দৈর্ঘ্য(পাথ.পরম_পাথ("তিন.txt")) > দৈর্ঘ্য("তিন.txt")))

}
