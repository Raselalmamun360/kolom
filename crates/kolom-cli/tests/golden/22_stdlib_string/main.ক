ইম্পোর্ট লেখা

অ্যাপ {

    ধরি ভাগ = লেখা.স্প্লিট("ক,খ,গ", ",")

    লেখো(দৈর্ঘ্য(ভাগ))

    লেখো(ভাগ[১])

    লেখো(লেখা.জুড়াও(ভাগ, "-"))

    লেখো(লেখায়(লেখা.খুঁজো("হ্যালো বিশ্ব", "বিশ্ব")))

    লেখো(লেখা.স্লাইস("কলম ভাষা", ০, ৪))

    লেখো(লেখা.বড়হাতের("abc"))

    লেখো(লেখায়(লেখা.ধারণ_করে("হ্যালো বিশ্ব", "বিশ্ব")))

    লেখো(লেখায়(লেখা.ধারণ_করে("হ্যালো বিশ্ব", "নেই")))

    লেখো(লেখা.পুনরাবৃত্তি("ab", ৩))

    লেখো(লেখা.উল্টানো("abc"))

}
