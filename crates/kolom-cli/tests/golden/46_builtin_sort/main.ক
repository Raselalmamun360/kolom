অ্যাপ {

    ধরি সংখ্যাগুলো = [৫, ২, ৯, ১, ৭, ৩]
    ধরি সাজানো = সাজাও(সংখ্যাগুলো)
    লেখো(সাজানো)
    লেখো(সংখ্যাগুলো)

    ধরি দশমিকগুলো = [3.1, 1.4, 2.7, 0.5]
    লেখো(সাজাও(দশমিকগুলো))

    ধরি শব্দগুলো = ["কলা", "আম", "জাম", "চাল"]
    লেখো(সাজাও(শব্দগুলো))

    ধরি একক = [৪২]
    লেখো(সাজাও(একক))

}
