ইম্পোর্ট গণিত

অ্যাপ {

    // খালি array literal — টাইপ টীকা দিয়ে
    ধরি খালি: সংখ্যা[] = []

    লেখো(লেখায়(দৈর্ঘ্য(খালি)))

    // বেয়ার স্ট্যান্ডার্ড লাইব্রেরি ধ্রুবক (কল নয়)
    লেখো(লেখায়(গণিত.পাই))

    লেখো(লেখায়(গণিত.ই))

}
