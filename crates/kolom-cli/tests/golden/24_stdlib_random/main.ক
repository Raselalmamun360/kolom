ইম্পোর্ট র‍্যান্ডম

অ্যাপ {

    র‍্যান্ডম.বীজ(৪২)

    লেখো(লেখায়(র‍্যান্ডম.মধ্যে(১, ১০)))

    লেখো(লেখায়(র‍্যান্ডম.মধ্যে(১, ১০)))

    লেখো(লেখায়(র‍্যান্ডম.মধ্যে(১, ১০)))

    // এলোমেলো_পছন্দ/এলোমেলো_মিশ্রণ-এর মান নির্দিষ্ট বীজেও ইন্টারপ্রেটার ও
    // কম্পাইলড কোডে আলাদা হতে পারে (দুটো ভিন্ন RNG ব্যবহার করে), তাই এখানে
    // শুধু RNG-নিরপেক্ষ বৈশিষ্ট্য যাচাই করা হয়েছে।

    // এক-উপাদানের অ্যারেতে ফলাফল নিশ্চিত
    ধরি একক: সংখ্যা[] = [৪২]

    লেখো(লেখায়(র‍্যান্ডম.এলোমেলো_পছন্দ(একক)))

    ধরি মিশ্রিত_একক = র‍্যান্ডম.এলোমেলো_মিশ্রণ(একক)

    লেখো(লেখায়(মিশ্রিত_একক[০]))

    // দৈর্ঘ্য অপরিবর্তিত থাকে
    ধরি মূল: সংখ্যা[] = [১, ২, ৩, ৪, ৫]

    লেখো(লেখায়(দৈর্ঘ্য(মূল)))

    ধরি মিশ্রিত = র‍্যান্ডম.এলোমেলো_মিশ্রণ(মূল)

    লেখো(লেখায়(দৈর্ঘ্য(মিশ্রিত)))

    // নন-মিউটেটিং — মূল অ্যারে অপরিবর্তিত থাকে
    লেখো(লেখায়(মূল[০]))

    লেখো(লেখায়(মূল[৪]))

    // এলিমেন্ট টাইপ-নির্বিশেষে কাজ করে (এখানে লেখা[])
    ধরি শব্দ: লেখা[] = ["ক"]

    লেখো(র‍্যান্ডম.এলোমেলো_পছন্দ(শব্দ))

    // খালি অ্যারে থেকে পছন্দ ধরার-যোগ্য ত্রুটি দেয় (arr[০] ইনডেক্সিং-এর মতোই)
    চেষ্টা {
        ধরি খালি: সংখ্যা[] = []
        ধরি x = র‍্যান্ডম.এলোমেলো_পছন্দ(খালি)
        লেখো(লেখায়(x))
    } ধরো(e) {
        লেখো("এলোমেলো_পছন্দ খালি অ্যারে ত্রুটি ধরা হয়েছে")
    }

}
