ইম্পোর্ট লেখা

অ্যাপ {

    ধরি বয়সসমূহ: ম্যাপ[লেখা, সংখ্যা] = ম্যাপ_তৈরি()

    বয়সসমূহ["রহিম"] = ২৫

    বয়সসমূহ["করিম"] = ৩০

    বয়সসমূহ["সেলিনা"] = ২৮

    লেখো(দৈর্ঘ্য(বয়সসমূহ))

    লেখো(লেখায়(বয়সসমূহ["রহিম"]))

    লেখো(লেখায়(বয়সসমূহ["করিম"]))

    // আপডেট
    বয়সসমূহ["রহিম"] = ২৬

    লেখো(লেখায়(বয়সসমূহ["রহিম"]))

    // আছে কি না
    লেখো(লেখায়(আছে_কি(বয়সসমূহ, "সেলিনা")))

    লেখো(লেখায়(আছে_কি(বয়সসমূহ, "অজানা")))

    // কী গুলো
    ধরি keys = চাবি_গুলো(বয়সসমূহ)

    লেখো(দৈর্ঘ্য(keys))

    // মুছে ফেলা
    চাবি_মুছো(বয়সসমূহ, "করিম")

    লেখো(দৈর্ঘ্য(বয়সসমূহ))

}
