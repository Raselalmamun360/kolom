ইম্পোর্ট গণিত

অ্যাপ {

    লেখো(লেখায়(গণিত.রাউন্ডঅফ(৩.৬)))

    লেখো(লেখায়(গণিত.পরম(-৫)))

    ধরি মূল = গণিত.বর্গমূল(১৬.০)

    লেখো(লেখায়(মূল))

    লেখো(লেখায়(গণিত.ছোটসংখ্যা(৭, ৩)))

    লেখো(লেখায়(গণিত.বেস(২.৯)))

    লেখো(লেখায়(গণিত.আপার(২.১)))

}
