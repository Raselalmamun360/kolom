ইম্পোর্ট eval
ইম্পোর্ট containers
ইম্পোর্ট dispatch
ইম্পোর্ট ffi

অ্যাপ {
    ধরি e = Add(শেয়ার_করো(Num(২)), শেয়ার_করো(Mul(শেয়ার_করো(Num(৩)), শেয়ার_করো(Num(৪)))))
    লেখো(লেখায়(মূল্যায়ন(e)))

    ধরি তালিকা = [১০, ২০, ৩০]
    ধরি প্রথমটা = প্রথম(তালিকা)
    লেখো(লেখায়(খুলুন(প্রথমটা, -১)))

    ধরি খালি: সংখ্যা[] = []
    ধরি খালি_ফল = প্রথম(খালি)
    লেখো(লেখায়(খুলুন(খালি_ফল, -১)))

    ধরি ফলাফল = প্রয়োগ_দুটি(৫, বর্গ, ঘন)
    লেখো(লেখায়(ফলাফল[০]))
    লেখো(লেখায়(ফলাফল[১]))

    লেখো(লেখায়(ফ্যাক্টোরিয়াল_মোড়ক(৬)))
}
