ডাটা ঠিকানা { শহর: লেখা, কোড: সংখ্যা }

ডাটা ব্যক্তি { নাম: লেখা, বাসা: ঠিকানা }

অ্যাপ {

    ধরি প = ব্যক্তি("রহিম", ঠিকানা("ঢাকা", ১২০৭))

    লেখো(প.নাম)

    লেখো(প.বাসা.শহর)

    লেখো(লেখায়(প.বাসা.কোড))

}
