ইম্পোর্ট ফাইল

অ্যাপ {

    // ফাইল নেই — ত্রুটি ধরা হবে
    চেষ্টা {
        ধরি content = ফাইল.পড়ো("নেই_এমন_ফাইল.txt")
        লেখো(content)
    } ধরো(e) {
        লেখো("ধরা হয়েছে:")
        লেখো(e)
    }

    // শূন্য দিয়ে ভাগ
    চেষ্টা {
        ধরি x = ১০ / ০
        লেখো(লেখায়(x))
    } ধরো(e) {
        লেখো("ভাগ ত্রুটি ধরা হয়েছে")
    }

    // সফল কোড চলবে
    লেখো("শেষ")

}
