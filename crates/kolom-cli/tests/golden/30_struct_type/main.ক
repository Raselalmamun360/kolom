তথ্য ব্যক্তি {
    নাম: লেখা,
    বয়স: সংখ্যা
}

অ্যাপ {

    ধরি p = ব্যক্তি("রহিম", ২৫)

    লেখো(p.নাম)

    লেখো(লেখায়(p.বয়স))

    // ফিল্ড পরিবর্তন
    p.বয়স = ২৬

    লেখো(লেখায়(p.বয়স))

}
