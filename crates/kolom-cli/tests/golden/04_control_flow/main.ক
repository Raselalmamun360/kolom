ফাংশন জোড়(সংখ্যা ক) -> সত্যতা {

    ফেরাও (ক % ২) == ০

}

অ্যাপ {

    ধরি গণনা = ০

    যতক্ষণ (গণনা < ৫) {

        যদি (জোড়(গণনা)) {

            লেখো("জোড়")

        } নাহলে {

            লেখো("বিজোড়")

        }

        গণনা = গণনা + ১

    }

}
