অ্যাপ {

    ধরি সংখ্যাসমূহ: সংখ্যা[] = [১০, ২০, ৩০]

    লেখো(দৈর্ঘ্য(সংখ্যাসমূহ))

    লেখো(সংখ্যাসমূহ[১])

    সংখ্যাসমূহ[০] = ৯৯

    লেখো(সংখ্যাসমূহ[০])

    ধরি ক = [১, ২]

    ধরি খ = [৩, ৪]

    ধরি গ = ক + খ

    লেখো(দৈর্ঘ্য(গ))

}
