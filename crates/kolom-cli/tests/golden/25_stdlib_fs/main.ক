ইম্পোর্ট ফাইল

ইম্পোর্ট ফাইলসিস্টেম

অ্যাপ {

    ফাইলসিস্টেম.ডিরেক্টরি_বানাও("উপতথ্য")

    ফাইল.লেখো("উপতথ্য/তথ্য.txt", "নমস্কার")

    লেখো(লেখায়(ফাইলসিস্টেম.ফাইল_আছে("উপতথ্য/তথ্য.txt")))

    ধরি তালিকা = ফাইলসিস্টেম.তালিকা("উপতথ্য")

    লেখো(দৈর্ঘ্য(তালিকা))

    ফাইলসিস্টেম.কপি("উপতথ্য/তথ্য.txt", "উপতথ্য/দুই.txt")

    ধরি তালিকা২ = ফাইলসিস্টেম.তালিকা("উপতথ্য")

    লেখো(দৈর্ঘ্য(তালিকা২))

    ফাইলসিস্টেম.সরাও("উপতথ্য/দুই.txt", "উপতথ্য/তিন.txt")

    লেখো(লেখায়(ফাইলসিস্টেম.ফাইল_আছে("উপতথ্য/তিন.txt")))

    লেখো(ফাইল.পড়ো("উপতথ্য/তথ্য.txt"))

}
