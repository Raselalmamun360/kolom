ইম্পোর্ট ফাইল

ইম্পোর্ট ফাইলসিস্টেম

অ্যাপ {

    ফাইলসিস্টেম.ডিরেক্টরি_বানাও("উপতথ্য২")

    ফাইল.লেখো("উপতথ্য২/তথ্য.txt", "ক")

    ফাইল.এপেন্ড("উপতথ্য২/তথ্য.txt", "খ")

    লেখো(ফাইল.পড়ো("উপতথ্য২/তথ্য.txt"))

    ফাইল.লেখো("উপতথ্য২/লাইন.txt", "এক\nদুই\nতিন")

    ধরি লাইনগুলো = ফাইল.লাইন_তালিকা("উপতথ্য২/লাইন.txt")

    লেখো(দৈর্ঘ্য(লাইনগুলো))

    লেখো(লাইনগুলো[০])

    লেখো(লাইনগুলো[২])

    লেখো(লেখায়(ফাইলসিস্টেম.আকার("উপতথ্য২/তথ্য.txt")))

    লেখো(লেখায়(ফাইলসিস্টেম.পরিবর্তনের_সময়("উপতথ্য২/তথ্য.txt") > ০))

    ফাইলসিস্টেম.ডিরেক্টরি_কপি("উপতথ্য২", "উপতথ্য২_কপি")

    লেখো(লেখায়(ফাইলসিস্টেম.ফাইল_আছে("উপতথ্য২_কপি/তথ্য.txt")))

    ফাইলসিস্টেম.ডিরেক্টরি_মুছো("উপতথ্য২_কপি")

    লেখো(লেখায়(ফাইলসিস্টেম.ডিরেক্টরি_আছে("উপতথ্য২_কপি")))

    ধরি বর্তমান = ফাইলসিস্টেম.বর্তমান_ডিরেক্টরি()

    লেখো(লেখায়(দৈর্ঘ্য(বর্তমান) > ০))

}
