ইম্পোর্ট জেসন

অ্যাপ {

    ধরি t = "{\"নাম\": \"রহিম\", \"বয়স\": 25}"

    লেখো(লেখায়(জেসন.বৈধকি(t)))

    লেখো(জেসন.লেখা_বের_করো(t, "নাম"))

    লেখো(লেখায়(জেসন.সংখ্যা_বের_করো(t, "বয়স")))

    লেখো(জেসন.বের_হও("লাইন১\nলাইন২"))

}
