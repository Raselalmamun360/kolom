// প্রথম কলম প্রোগ্রাম — ভ্যারিয়েবল, বেসিক টাইপ, এবং লেখো()

অ্যাপ {

    ধরি নাম = "বিশ্ব"

    লেখো("হ্যালো, " + নাম + "!")

    ধরি বয়স = ২৫
    ধরি ওজন = ৬৮.৫
    ধরি ছাত্র_কি = সত্য

    লেখো("বয়স: " + লেখায়(বয়স))
    লেখো("ওজন: " + লেখায়(ওজন))
    লেখো("ছাত্র: " + লেখায়(ছাত্র_কি))

}
