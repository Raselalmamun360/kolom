ইম্পোর্ট টোকেন
ইম্পোর্ট শ্রেণি
ইম্পোর্ট নরমাল

অ্যাপ {
    লেখো(লেখায়(যেকোনো_অঙ্ক_কিনা('৭')) + লেখায়(যেকোনো_অঙ্ক_কিনা('7')) + লেখায়(যেকোনো_অঙ্ক_কিনা('ক')))
    লেখো(লেখায়(নাম_শুরু_কিনা('ক')) + লেখায়(নাম_শুরু_কিনা('_')) + লেখায়(নাম_শুরু_কিনা('৭')) + লেখায়(নাম_শুরু_কিনা('\u{09F3}')))
    লেখো(লেখায়(নাম_অংশ_কিনা('৭')) + লেখায়(নাম_অংশ_কিনা('ক')) + লেখায়(নাম_অংশ_কিনা(' ')))
    লেখো(লেখায়(অঙ্কের_মান('৭')) + " " + লেখায়(অঙ্কের_মান('7')))
    লেখো(অঙ্ক_ascii_তে('৫') + অঙ্ক_ascii_তে('5'))
    লেখো(লেখায়(দৈর্ঘ্য(নরমাল("শে\u{09DF}ার"))) + " " + লেখায়(নরমাল("শে\u{09DF}ার") == "শেয়ার"))
}
